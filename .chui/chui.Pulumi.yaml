name: {{globalAppName}}-core
runtime: nodejs
description: Deploy Minio to a Chui cluster.
template:
  minioAccessKey:
    description: The access key to use with S3-compatible Minio config.
    secret: true
  minioSecretKey:
    description: The secret key to use with S3-compatible Minio config.
    secret: true